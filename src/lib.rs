pub mod real_number {
    /// this number can show fraction number
    /// TODO: to express any number as a fraction
    pub mod rational {
        use fraction_rs::Fraction;

    }

    /// this number cannot show fraction number
    /// TODO: create square root format and other irrational number
    pub mod irrational {
        const SQUARE_ROOT2 = ;
    }
}
